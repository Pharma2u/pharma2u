import type { ReactNode } from "react";
import { CheckCircle2, Clock3, RefreshCw } from "lucide-react";
export function TaskList({ title, description, count, emptyText, emptyIcon, onRefresh, children }: { title: string; description: string; count: number; emptyText: string; emptyIcon: "check" | "clock"; onRefresh?: () => void; children: ReactNode }) { const EmptyIcon = emptyIcon === "check" ? CheckCircle2 : Clock3; return <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><header className="flex items-center justify-between border-b border-slate-100 p-4"><div><div className="flex items-center gap-2"><h2 className="font-bold text-slate-900">{title}</h2><span className="rounded-full bg-slate-100 px-2 py-0.5 text-xs font-bold text-slate-600">{count}</span></div><p className="mt-1 text-xs text-slate-500">{description}</p></div>{onRefresh && <button type="button" onClick={onRefresh} aria-label={`Refresh ${title}`} className="rounded-xl border border-slate-200 p-2 text-slate-500 hover:bg-slate-50"><RefreshCw size={17} /></button>}</header>{count ? <div className="space-y-3 bg-slate-50/60 p-3 sm:p-4">{children}</div> : <div className="px-5 py-12 text-center"><span className="mx-auto grid h-12 w-12 place-items-center rounded-2xl bg-slate-100 text-slate-400"><EmptyIcon size={22} /></span><strong className="mt-3 block text-sm text-slate-700">{emptyIcon === "check" ? "All clear" : "Nothing waiting"}</strong><p className="mx-auto mt-1 max-w-xs text-xs leading-5 text-slate-400">{emptyText}</p></div>}</section>; }
